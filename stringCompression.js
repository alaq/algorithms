/**
 * @param {character[]} chars
 * @return {number}
 */

var compress = function(chars) {
    let i = 1
    let current = chars[0]
    let count = 0
    while (i < chars.length) {
        current = chars[i]
        while (chars[i + c + 1] === current) {
            c++
            cha 
        }
    }
};
